<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pessoa ? 'Editar Pessoa' : 'Adicionar Pessoa' %></title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/js/script.js"></script>
</head>
<body>
  <%- include('partials/header') %>
  <main class="container">
  <h1><%= pessoa ? 'Editar Pessoa' : 'Adicionar Pessoa' %></h1>
  <form action="<%= pessoa ? '/pessoas/editar/' + pessoa.id : '/pessoas' %>" method="POST">
    <label for="nome">Nome:</label>
    <input type="text" id="nome" name="nome" value="<%= pessoa ? pessoa.nome : '' %>" required>
    <br>

    <label for="data_nascimento">Data de Nascimento:</label>
    <input type="date" id="data_nascimento" name="data_nascimento" value="<%= pessoa ? pessoa.data_nascimento : '' %>" required>
    <br>

    <label for="sexo">Sexo:</label>
    <select id="sexo" name="sexo" required>
        <option value="Masculino" <%= pessoa && pessoa.sexo === 'Masculino' ? 'selected' : '' %>>Masculino</option>
        <option value="Feminino" <%= pessoa && pessoa.sexo === 'Feminino' ? 'selected' : '' %>>Feminino</option>
        <option value="Outro" <%= pessoa && pessoa.sexo === 'Outro' ? 'selected' : '' %>>Outro</option>
    </select>
    <br>

    <label for="nacionalidade">Nacionalidade:</label>
    <input type="text" id="nacionalidade" name="nacionalidade" value="<%= pessoa ? pessoa.nacionalidade : '' %>" required>
    <br>

    <label for="tipo">Tipo:</label>
    <select id="tipo" name="tipo" required>
        <option value="Ator" <%= pessoa && pessoa.tipo === 'Ator' ? 'selected' : '' %>>Ator</option>
        <option value="Diretor" <%= pessoa && pessoa.tipo === 'Diretor' ? 'selected' : '' %>>Diretor</option>
        <option value="Produtor" <%= pessoa && pessoa.tipo === 'Produtor' ? 'selected' : '' %>>Produtor</option>
    </select>
    <br>

    <button type="submit">Salvar</button>
</form>
</main>
  <%- include('partials/toggle') %>

</body>
</html>
