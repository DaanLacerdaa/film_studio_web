<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= produtor ? 'Editar Produtor' : 'Adicionar Produtor' %></title>
  <link rel="stylesheet" href="/css/style.css">
  <script src="/js/script.js"></script>

</head>
<body>
  <%- include('partials/header') %>
  <main class="container">
  <h1><%= produtor ? 'Editar Produtor' : 'Adicionar Produtor' %></h1>
  <form action="<%= produtor ? '/pessoas/editar/' + produtor.id : '/pessoas/produtores' %>" method="POST">
  </form>
    <label for="nome">Nome:</label>
    <input type="text" id="nome" name="nome" value="<%= produtor ? produtor.nome : '' %>" required>
    <br>
    <label for="data_nascimento">Data de Nascimento:</label>
    <input type="date" id="data_nascimento" name="data_nascimento" value="<%= produtor ? produtor.data_nascimento : '' %>" required>
    <br>
    <label for="sexo">Sexo:</label>
    <select id="sexo" name="sexo" required>
      <option value="Masculino" <%= produtor && produtor.sexo === 'Masculino' ? 'selected' : '' %>>Masculino</option>
      <option value="Feminino" <%= produtor && produtor.sexo === 'Feminino' ? 'selected' : '' %>>Feminino</option>
      <option value="Outro" <%= produtor && produtor.sexo === 'Outro' ? 'selected' : '' %>>Outro</option>
    </select>
    <br>
    <label for="nacionalidade">Nacionalidade:</label>
    <input type="text" id="nacionalidade" name="nacionalidade" value="<%= produtor ? produtor.nacionalidade : '' %>" required>
    <br>
    <button type="submit">Salvar</button>
  </form>
  </main>
  <%- include('partials/toggle') %>

</body>
</html>
