<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Estúdio de Filmes</title>
    <link rel="stylesheet" href="/css/style.css" />
    <style>
      body {
        margin: 0;
        padding: 0;
        font-family: Arial, sans-serif;
        background: url('/images/poster-bg.jpg') center/cover no-repeat fixed;
        color: white;
        text-align: center;
      }
      .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.6);
      }
      .hero {
        position: relative;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
        z-index: 2;
      }
      .hero h1 {
        font-size: 3rem;
        font-weight: bold;
        margin-bottom: 10px;
      }
      .hero p {
        font-size: 1.5rem;
        max-width: 600px;
      }
      .cta-button {
        display: inline-block;
        margin-top: 20px;
        padding: 12px 30px;
        background-color: #e50914;
        color: white;
        font-size: 1.2rem;
        text-decoration: none;
        border-radius: 5px;
        transition: background 0.3s;
      }
      .cta-button:hover {
        background-color: #b20710;
      }
    </style>
  </head>
  <body>
    <div class="overlay"></div>
    <%- include('partials/header') %>
    <main class="hero">
      <h1>Bem-vindo ao Estúdio de Produção de Filmes</h1>
      <h2>Explore o mundo do cinema com produções inesquecíveis e histórias emocionantes.</h2>
      <a href="/filmes" class="cta-button">Conheça Nossos Filmes</a>
    </main>
    <%- include('partials/toggle') %>
  </body>
</html>
